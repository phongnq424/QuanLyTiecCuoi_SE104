<Page x:Class="QuanLyTiecCuoi.Tiec_tracuu"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:QuanLyTiecCuoi"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Tiec_tracuu">


    <Grid Margin="80,0,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="3*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock 
                Text="Serenité Wedding" 
                FontSize="30" 
                FontStyle="Italic" 
                FontWeight="SemiBold" 
                Margin="0 10 20 0"/>
        <StackPanel 
                Orientation="Horizontal" 
                HorizontalAlignment="Center"
                Margin="0 70 0 10">
            <ComboBox 
                Width="200" 
                Height="40" 
                Margin="0 10 30 10">
                <ComboBoxItem Content="Món 1" />
                <ComboBoxItem Content="Món 2" />
                <ComboBoxItem Content="Món 3" />
            </ComboBox>
            <TextBox
                    Text="Nhap tim kiem"
                    TextAlignment="Center"
                    VerticalContentAlignment="Center"
                    FontStyle="Italic" 
                    FontSize="18" 
                    Margin="0 10 20 10"
                    Width="200"
                    BorderThickness="1"
                    GotFocus="TextBox_GotFocus"
                    LostFocus="TextBox_LostFocus"/>
        </StackPanel>

        <Border Background="#F8F4EF" 
            CornerRadius="10" 
            Padding="15" 
            BorderBrush="Gray" 
            BorderThickness="1"
            Margin="0,7,0,12"
            Width="690"
            HorizontalAlignment="Left" Grid.Row="2">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="179*"/>
                    <RowDefinition Height="39*"/>
                </Grid.RowDefinitions>
                <ListView Name="DanhSachDatTiec" Grid.RowSpan="2">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Cô dâu" Width="60" DisplayMemberBinding="{Binding CoDau}" />
                            <GridViewColumn Header="Chú rể" Width="60" DisplayMemberBinding="{Binding ChuRe}" />
                            <GridViewColumn Header="SĐT" Width="70" DisplayMemberBinding="{Binding SoDienThoai}" />
                            <GridViewColumn Header="Đặt cọc" Width="70" DisplayMemberBinding="{Binding TienDatCoc}" />
                            <GridViewColumn Header="Ca" Width="80" DisplayMemberBinding="{Binding Ca}" />
                            <GridViewColumn Header="Sảnh" Width="60" DisplayMemberBinding="{Binding Sanh}" />
                            <GridViewColumn Header="Ngày đãi" Width="80" DisplayMemberBinding="{Binding NgayDaiTiec}" />
                            <GridViewColumn Header="Số bàn" Width="60" DisplayMemberBinding="{Binding SoBan}" />
                            <GridViewColumn Header="Số bàn dự trữ" Width="80" DisplayMemberBinding="{Binding SBDT}" />
                            <GridViewColumn Header="  " Width="80" DisplayMemberBinding="{Binding SBDT}" />
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
        </Border>
        <StackPanel Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,10,10,0" 
                    Grid.Row="3">
            <Button Content="Thêm tiệc cưới" Width="100" Height="30" Margin="0 10 20 10" Click="Add_Click"/>
            <Button Content="In hóa đơn" Width="100" Height="30" Margin="0 10 20 10" Click="InHoaDon_Click"/>
        </StackPanel>
    </Grid>
</Page>
